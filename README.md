TODO: Readme

1. Run `init.bat` - it will download all needed python packages with UV and install it locally.
2. Run `run.bat` - it will launch application in a window + system tray icon.

NOTE: all python packages, persistent data and caches will be stored at workdir, so keep this in mind when creating a link to the `run.bat` file.
